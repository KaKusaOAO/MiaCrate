using MiaCrate.Core;

namespace MiaCrate.Client.Models;

public sealed class CubeDefinition
{
    public CubeDefinition(string? comment, float f, float g, float h, float i, float j, float k, float l, float m,
        CubeDeformation cubeDeformation, bool bl, float n, float o, HashSet<Direction> set)
    {
        
    }
}